/**
 * @kmkt/domain — the core business domain of KMKT Social AI.
 *
 * INTENTIONALLY EMPTY for SPRINT 001 (Technical Bootstrap).
 *
 * This package will hold the platform-agnostic domain concepts described in
 * docs/06-domain-model.md (User, Workspace, Business, Business Profile,
 * Platform, Platform Account, Facebook Group, Business Group Assignment, Post,
 * Business Match, Comment Draft, Approval Decision, Comment Job, Comment
 * Attempt, Screenshot Evidence, Telegram Destination, Audit Event, Kill
 * Switch). No entities are defined yet — this is structure only.
 *
 * Design rule (see docs/07-system-overview.md): the domain is the CORE and must
 * stay independent of Facebook, Playwright, Telegram, AI, and any framework.
 */

export const DOMAIN_PACKAGE = '@kmkt/domain';
