{
  "name": "@kmkt/facebook-scanner",
  "version": "0.0.0",
  "private": true,
  "description": "Facebook group scanner worker (READ-ONLY, reserved). Disabled placeholder — no Playwright, no execution in the bootstrap.",
  "type": "commonjs",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
