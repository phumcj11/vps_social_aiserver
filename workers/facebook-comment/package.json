{
  "name": "@kmkt/facebook-comment",
  "version": "0.0.0",
  "private": true,
  "description": "Facebook comment executor worker (WRITE, reserved). Disabled placeholder — no Playwright, no execution, gated by kill switch in the bootstrap.",
  "type": "commonjs",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
